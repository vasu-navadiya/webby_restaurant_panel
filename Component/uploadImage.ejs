<!-- <!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Restaurant Gallery Upload</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet">
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.4.0/css/all.min.css">
    <style>
        :root {
            --primary-gradient: linear-gradient(135deg, #667eea 0%, #764ba2 100%);
            --primary-color: #667eea;
            --secondary-color: #764ba2;
        }

        body {
            background-color: #f8f9fa;
            font-family: 'Segoe UI', Tahoma, Geneva, Verdana, sans-serif;
            padding-top: 2rem;
        }

        .upload-container {
            max-width: 800px;
            margin: 2rem auto;
            background: white;
            border-radius: 15px;
            box-shadow: 0 5px 20px rgba(0, 0, 0, 0.1);
            padding: 2rem;
        }

        .page-header {
            color: var(--secondary-color);
            margin-bottom: 2rem;
            padding-bottom: 1rem;
            border-bottom: 2px solid #eee;
            position: relative;
        }

        .page-header::after {
            content: '';
            position: absolute;
            bottom: -2px;
            left: 0;
            width: 60px;
            height: 2px;
            background: var(--primary-gradient);
        }

        .page-header i {
            color: var(--secondary-color);
        }

        .upload-box {
            border: 2px dashed #e2e8f0;
            border-radius: 10px;
            padding: 1.5rem;
            margin-bottom: 1.5rem;
            transition: all 0.3s ease;
            background: #f7fafc;
        }

        .upload-box:hover {
            border-color: var(--primary-color);
            box-shadow: 0 0 0 3px rgba(102, 126, 234, 0.1);
        }

        .input-label {
            font-weight: 600;
            color: #2d3748;
            margin-bottom: 0.75rem;
            display: block;
        }

        .form-control {
            border: 1px solid #e2e8f0;
            border-radius: 10px;
            padding: 0.75rem 1rem;
            transition: all 0.3s ease;
        }

        .form-control:focus {
            border-color: var(--primary-color);
            box-shadow: 0 0 0 3px rgba(102, 126, 234, 0.1);
        }

        .preview-container {
            width: 100%;
            height: 160px;
            background: white;
            border-radius: 10px;
            overflow: hidden;
            display: flex;
            align-items: center;
            justify-content: center;
            margin-top: 1rem;
            border: 1px solid #e2e8f0;
        }

        .preview-placeholder {
            color: #718096;
            text-align: center;
        }

        .preview-placeholder i {
            font-size: 2.5rem;
            margin-bottom: 0.75rem;
            color: var(--primary-color);
        }

        .submit-btn {
            background: var(--primary-gradient);
            border: none;
            padding: 0.8rem 2rem;
            border-radius: 10px;
            font-weight: 600;
            transition: all 0.3s ease;
        }

        .submit-btn:hover {
            transform: translateY(-2px);
            box-shadow: 0 5px 15px rgba(102, 126, 234, 0.4);
        }

        .remove-btn {
            position: absolute;
            top: 8px;
            right: 8px;
            background: rgba(255,255,255,0.95);
            border: none;
            border-radius: 50%;
            width: 28px;
            height: 28px;
            font-size: 14px;
            display: flex;
            align-items: center;
            justify-content: center;
            cursor: pointer;
            color: #e53e3e;
            opacity: 0;
            transition: all 0.3s ease;
            box-shadow: 0 2px 6px rgba(0,0,0,0.15);
        }

        .preview-container:hover .remove-btn {
            opacity: 1;
        }

        .preview-container img {
            width: 100%;
            height: 100%;
            object-fit: cover;
        }

        .upload-info {
            font-size: 0.875rem;
            color: #718096;
            margin-top: 0.5rem;
        }
    </style>
</head>
<body>
    <div class="container">
        <div class="upload-container">
            <div class="page-header">
                <h2 class="mb-0">
                    <i class="fas fa-images me-2"></i>
                    Upload Gallery Images
                </h2>
            </div>

            <!-- <form id="uploadForm" action="/upload-gallery" method="POST" enctype="multipart/form-data">
                <div class="row g-3">
                     Front View -->

                    <!-- <div class="col-md-6">
                        <div class="upload-box">
                            <label class="input-label">
                                <i class="fas fa-store-alt me-1"></i>
                                Restaurant Front
                            </label>
                            <input type="file" class="form-control" name="frontView" accept="image/*" onchange="previewImage(this, 'preview1')">
                            <div class="preview-container" id="preview1">
                                <div class="preview-placeholder">
                                    <i class="fas fa-image"></i>
                                    <p class="mb-0 small">Front view</p>
                                </div>
                            </div>
                        </div>
                    </div> -->

                    <!-- Interior View 
                    <div class="col-md-6">
                        <div class="upload-box">
                            <label class="input-label">
                                <i class="fas fa-door-open me-1"></i>
                                Interior View
                            </label>
                            <input type="file" class="form-control" name="interiorView" accept="image/*" onchange="previewImage(this, 'preview2')">
                            <div class="preview-container" id="preview2">
                                <div class="preview-placeholder">
                                    <i class="fas fa-image"></i>
                                    <p class="mb-0 small">Interior view</p>
                                </div>
                            </div>
                        </div>
                    </div>

                    <!-- Dining Area 
                    <div class="col-md-6">
                        <div class="upload-box">
                            <label class="input-label">
                                <i class="fas fa-utensils me-1"></i>
                                Dining Area
                            </label>
                            <input type="file" class="form-control" name="diningView" accept="image/*" onchange="previewImage(this, 'preview3')">
                            <div class="preview-container" id="preview3">
                                <div class="preview-placeholder">
                                    <i class="fas fa-image"></i>
                                    <p class="mb-0 small">Dining area</p>
                                </div>
                            </div>
                        </div>
                    </div>

                    <!-- Kitchen View 
                    <div class="col-md-6">
                        <div class="upload-box">
                            <label class="input-label">
                                <i class="fas fa-kitchen-set me-1"></i>
                                Kitchen View
                            </label>
                            <input type="file" class="form-control" name="kitchenView" accept="image/*" onchange="previewImage(this, 'preview4')">
                            <div class="preview-container" id="preview4">
                                <div class="preview-placeholder">
                                    <i class="fas fa-image"></i>
                                    <p class="mb-0 small">Kitchen view</p>
                                </div>
                            </div>
                        </div>
                    </div>

                    <!-- Special Features 
                    <div class="col-md-6">
                        <div class="upload-box">
                            <label class="input-label">
                                <i class="fas fa-star me-1"></i>
                                Special Feature 1
                            </label>
                            <input type="file" class="form-control" name="special1View" accept="image/*" onchange="previewImage(this, 'preview5')">
                            <div class="preview-container" id="preview5">
                                <div class="preview-placeholder">
                                    <i class="fas fa-image"></i>
                                    <p class="mb-0 small">Special feature</p>
                                </div>
                            </div>
                        </div>
                    </div>

                    <div class="col-md-6">
                        <div class="upload-box">
                            <label class="input-label">
                                <i class="fas fa-star me-1"></i>
                                Special Feature 2
                            </label>
                            <input type="file" class="form-control" name="special2View" accept="image/*" onchange="previewImage(this, 'preview6')">
                            <div class="preview-container" id="preview6">
                                <div class="preview-placeholder">
                                    <i class="fas fa-image"></i>
                                    <p class="mb-0 small">Special feature</p>
                                </div>
                            </div>
                        </div>
                    </div>
                </div>

                <div class="text-end mt-4">
                    <button type="submit" class="btn btn-primary submit-btn">
                        <i class="fas fa-upload me-2"></i>Upload Gallery
                    </button>
                </div>
            </form> -->
            <form id="uploadForm" action="/upload-gallery" method="POST" enctype="multipart/form-data">
                <div class="row g-3">
                    <!-- Image Upload Boxes (Now using galleryImages[]) -->
                    <div class="col-md-6">
                        <div class="upload-box">
                            <label class="input-label">
                                <i class="fas fa-store-alt me-1"></i> Restaurant Front
                            </label>
                            <input type="file" class="form-control" name="galleryImages" accept="image/*" onchange="previewImage(this, 'preview1')">
                            <div class="preview-container" id="preview1">
                                <div class="preview-placeholder">
                                    <i class="fas fa-image"></i>
                                    <p class="mb-0 small">Front view</p>
                                </div>
                            </div>
                        </div>
                    </div>
            
                    <div class="col-md-6">
                        <div class="upload-box">
                            <label class="input-label">
                                <i class="fas fa-door-open me-1"></i> Interior View
                            </label>
                            <input type="file" class="form-control" name="galleryImages" accept="image/*" onchange="previewImage(this, 'preview2')">
                            <div class="preview-container" id="preview2">
                                <div class="preview-placeholder">
                                    <i class="fas fa-image"></i>
                                    <p class="mb-0 small">Interior view</p>
                                </div>
                            </div>
                        </div>
                    </div>
            
                    <div class="col-md-6">
                        <div class="upload-box">
                            <label class="input-label">
                                <i class="fas fa-utensils me-1"></i> Dining Area
                            </label>
                            <input type="file" class="form-control" name="galleryImages" accept="image/*" onchange="previewImage(this, 'preview3')">
                            <div class="preview-container" id="preview3">
                                <div class="preview-placeholder">
                                    <i class="fas fa-image"></i>
                                    <p class="mb-0 small">Dining area</p>
                                </div>
                            </div>
                        </div>
                    </div>
            
                    <div class="col-md-6">
                        <div class="upload-box">
                            <label class="input-label">
                                <i class="fas fa-kitchen-set me-1"></i> Kitchen View
                            </label>
                            <input type="file" class="form-control" name="galleryImages" accept="image/*" onchange="previewImage(this, 'preview4')">
                            <div class="preview-container" id="preview4">
                                <div class="preview-placeholder">
                                    <i class="fas fa-image"></i>
                                    <p class="mb-0 small">Kitchen view</p>
                                </div>
                            </div>
                        </div>
                    </div>
            
                    <div class="col-md-6">
                        <div class="upload-box">
                            <label class="input-label">
                                <i class="fas fa-star me-1"></i> Special Feature 1
                            </label>
                            <input type="file" class="form-control" name="galleryImages" accept="image/*" onchange="previewImage(this, 'preview5')">
                            <div class="preview-container" id="preview5">
                                <div class="preview-placeholder">
                                    <i class="fas fa-image"></i>
                                    <p class="mb-0 small">Special feature</p>
                                </div>
                            </div>
                        </div>
                    </div>
            
                    <div class="col-md-6">
                        <div class="upload-box">
                            <label class="input-label">
                                <i class="fas fa-star me-1"></i> Special Feature 2
                            </label>
                            <input type="file" class="form-control" name="galleryImages" accept="image/*" onchange="previewImage(this, 'preview6')">
                            <div class="preview-container" id="preview6">
                                <div class="preview-placeholder">
                                    <i class="fas fa-image"></i>
                                    <p class="mb-0 small">Special feature</p>
                                </div>
                            </div>
                        </div>
                    </div>
                </div>
            
                <div class="text-end mt-4">
                    <button type="submit" class="btn btn-primary submit-btn">
                        <i class="fas fa-upload me-2"></i>Upload Gallery
                    </button>
                </div>
    </form>
            
        </div>
    </div>

    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/js/bootstrap.bundle.min.js"></script>
    <script>
        function previewImage(input, previewId) {
            const preview = document.getElementById(previewId);
            
            if (input.files && input.files[0]) {
                const reader = new FileReader();
                
                reader.onload = function(e) {
                    preview.innerHTML = `
                        <img src="${e.target.result}" alt="Preview">
                        <button type="button" class="remove-btn" onclick="removeImage('${previewId}', '${input.name}')">
                            <i class="fas fa-times"></i>
                        </button>
                    `;
                }
                
                reader.readAsDataURL(input.files[0]);
            }
        }

        function removeImage(previewId, inputName) {
            const preview = document.getElementById(previewId);
            const input = document.querySelector(`input[name="${inputName}"]`);
            const icon = preview.getAttribute('data-icon') || 'fa-image';
            
            preview.innerHTML = `
                <div class="preview-placeholder">
                    <i class="fas ${icon}"></i>
                    <p class="mb-0 small">Image preview</p>
                </div>
            `;
            input.value = '';
        }

        // document.getElementById('uploadForm').addEventListener('submit', async function(e) {
        //     e.preventDefault();
        //     const formData = new FormData(this);
            
        //     try {
        //         const response = await fetch('/upload-gallery', {
        //             method: 'POST',
        //             body: formData
        //         });
                
        //         const result = await response.json();
                
        //         if (response.ok) {
        //             alert('Gallery images uploaded successfully!');
        //             window.location.href = '/Dashboard';
        //         } else {
        //             alert('Upload failed: ' + result.message);
        //         }
        //     } catch (error) {
        //         alert('An error occurred while uploading images.');
        //         console.error('Upload Error:', error);
        //     }
        // });
//     </script>
// </body>
// </html> 
<div class="form-section image-gallery-section">
    <div class="section-header">
      <h3>Restaurant Images</h3>
      <p class="section-description">Upload high-quality photos to showcase your restaurant</p>
    </div>
    
    <div class="row image-upload-container">
      <!-- Interior View -->
      <div class="col-md-6">
        <div class="upload-box">
          <label class="input-label">
            <i class="fas fa-door-open me-1"></i> Interior View
          </label>
          <input type="file" class="form-control" name="galleryImages" accept="image/*" onchange="previewImage(this, 'preview2')">
         
        </div>
      </div>
      
      <!-- Dining Area -->
      <div class="col-md-6">
        <div class="upload-box">
          <label class="input-label">
            <i class="fas fa-utensils me-1"></i> Dining Area
          </label>
          <input type="file" class="form-control" name="galleryImages" accept="image/*" onchange="previewImage(this, 'preview3')">
        
        </div>
      </div>
      
      <!-- Kitchen View -->
      <div class="col-md-6">
        <div class="upload-box">
          <label class="input-label">
            <i class="fas fa-kitchen-set me-1"></i> Kitchen View
          </label>
          <input type="file" class="form-control" name="galleryImages" accept="image/*" onchange="previewImage(this, 'preview4')">
          
        </div>
      </div>
      
      <!-- Special Feature 1 -->
      <div class="col-md-6">
        <div class="upload-box">
          <label class="input-label">
            <i class="fas fa-star me-1"></i> Special Feature 1
          </label>
          <input type="file" class="form-control" name="galleryImages" accept="image/*" onchange="previewImage(this, 'preview5')">
          
        </div>
      </div>
      
      <!-- Special Feature 2 -->
      <div class="col-md-6">
        <div class="upload-box">
          <label class="input-label">
            <i class="fas fa-star me-1"></i> Special Feature 2
          </label>
          <input type="file" class="form-control" name="galleryImages" accept="image/*" onchange="previewImage(this, 'preview6')">
         
          
        </div>
      </div>
    </div>
  </div>